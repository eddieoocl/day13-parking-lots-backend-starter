spring:
  application:
    name: "ParkingLots"
  datasource:
    url: jdbc:mysql://localhost:3306/parkinglots_db
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: "root"
    password: "admin"
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update